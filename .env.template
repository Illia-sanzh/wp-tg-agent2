# ─────────────────────────────────────────────────────────────────────────────
# WordPress Telegram Agent — Environment Variables
# Copy this to .env and fill in your values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Provider (at least one required) ──────────────────────────────────────
# Anthropic (Claude) — get key at https://console.anthropic.com
ANTHROPIC_API_KEY=

# OpenAI (ChatGPT) — get key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# DeepSeek — get key at https://platform.deepseek.com
DEEPSEEK_API_KEY=

# Google Gemini — get key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ── Model Selection ───────────────────────────────────────────────────────────
# Which model to use by default. Must match a model_name in litellm/config.yaml
# Options: claude-sonnet-4-6, gpt-4o, deepseek-chat, gemini-2.0-flash
DEFAULT_MODEL=claude-sonnet-4-6

# Fallback model if default fails
FALLBACK_MODEL=deepseek-chat

# ── LiteLLM Internal Auth ─────────────────────────────────────────────────────
# This is the key the agent uses to call LiteLLM (NOT an external API key).
# Generate with: openssl rand -hex 32
# Must start with "sk-"
LITELLM_MASTER_KEY=sk-CHANGE-ME-USE-OPENSSL-RAND-HEX-32

# ── Budget Guard ──────────────────────────────────────────────────────────────
# Monthly spend limit in USD. LiteLLM rejects requests when exceeded.
MONTHLY_BUDGET_USD=20

# ── Telegram ─────────────────────────────────────────────────────────────────
# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Your Telegram user ID (only this user can send commands)
# Find yours by messaging @userinfobot
TELEGRAM_ADMIN_USER_ID=

# ── WordPress Connection ──────────────────────────────────────────────────────
# The public URL of your WordPress site
WP_URL=https://yoursite.com

# Admin credentials (used for REST API and WP-CLI via bridge plugin)
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=

# Application Password (recommended over admin password for REST API)
# Create at: WP Admin → Users → Your Profile → Application Passwords
WP_APP_PASSWORD=

# Path to WordPress on THIS server (leave empty if WordPress is on a different server)
# If set, WP-CLI runs directly in the container (faster, more capabilities)
WP_PATH=/var/www/html

# ── WordPress Bridge Plugin ────────────────────────────────────────────────────
# A secret token used by the bridge plugin for authentication.
# Generate with: openssl rand -hex 32
BRIDGE_SECRET=CHANGE-ME-USE-OPENSSL-RAND-HEX-32
